#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define INFINTY
int main()
{
    int contador = 0;
    float MaiorPar = -INFINTY, MenorPar = +INFINTY, MaiorImpar = -INFINTY, MenorImpar = +INFINTY;
    printf("Maiores e Menores pares e impares:\n");
    while(contador < 300){




















        contador++;
    }
    return 0;
}
